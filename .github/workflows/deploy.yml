script: |
  set -euo pipefail
  cd /opt/myapp

  ls -lah
  ls -lah target

  JAR="$(ls -t target/*.jar | head -n 1)"
  echo "Using $JAR"
  cp "$JAR" app.jar

  sudo -n systemctl daemon-reload
  sudo -n systemctl restart myapp
  sudo -n systemctl status myapp --no-pager